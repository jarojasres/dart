main() {
  //Single line.
  greetings(1, "Hello!");
}

// Comments for documentation.
/// Greet function.
///
/// Parameters [code] and [message].
greetings(int code, String message) {
  print('${code}-${message}');
}
